package com.ptms.dto; import com.ptms.domain.TicketStatus; import jakarta.validation.constraints.*; import java.time.*; public record TicketDto(Long id,@NotBlank String barcode,@Min(1) int durationHours,@Min(1) Integer ticketNumber,TicketStatus status,LocalDate expiryDate,LocalDateTime createdAt,LocalDateTime issuedAt,String issuedToReference){}
