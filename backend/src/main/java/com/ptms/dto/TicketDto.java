package com.ptms.dto; import com.ptms.domain.TicketStatus; import jakarta.validation.constraints.*; import java.time.*; public record TicketDto(Long id,@NotBlank String barcode,@Min(1) int durationHours,TicketStatus status,LocalDate expiryDate,LocalDateTime createdAt,LocalDateTime issuedAt,String issuedToReference){}
