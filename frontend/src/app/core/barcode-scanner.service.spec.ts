import {TestBed} from '@angular/core/testing';import {BarcodeScannerService} from './barcode-scanner.service';describe('BarcodeScannerService',()=>{it('emits scanner buffer on Enter',(done)=>{const s=TestBed.inject(BarcodeScannerService);s.enable();s.scanned$.subscribe(v=>{expect(v).toBe('01H01');done();});for(const c of '01H01')window.dispatchEvent(new KeyboardEvent('keydown',{key:c}));window.dispatchEvent(new KeyboardEvent('keydown',{key:'Enter'}));});});
