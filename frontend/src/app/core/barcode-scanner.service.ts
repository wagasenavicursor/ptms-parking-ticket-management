import {Injectable,NgZone} from '@angular/core';import {Subject} from 'rxjs';
/** Reusable HID-keyboard barcode scanner adapter. */
@Injectable({providedIn:'root'}) export class BarcodeScannerService{private out=new Subject<string>();readonly scanned$=this.out.asObservable();private buffer='';private last=0;private enabled=false;constructor(private zone:NgZone){window.addEventListener('keydown',e=>this.onKey(e));}enable(){this.enabled=true;this.buffer='';}disable(){this.enabled=false;this.buffer='';}private onKey(e:KeyboardEvent){if(!this.enabled)return;const now=Date.now();if(e.key==='Enter'){if(this.buffer){e.preventDefault();const b=this.buffer;this.buffer='';this.zone.run(()=>this.out.next(b));}return;}if(e.key.length===1){if(now-this.last>80)this.buffer='';this.buffer+=e.key;this.last=now;}}}
